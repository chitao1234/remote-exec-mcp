# MSVC/NMAKE entry point for remote-exec-daemon-cpp.
#
# Host-native MSVC build from a Visual Studio developer prompt:
#   nmake /f NMakefile check-msvc-native
#
# Windows XP-compatible MSVC C++11 build from an x86 Visual Studio developer
# prompt with an XP-capable toolset, for example:
#   vcvarsall.bat x86 -vcvars_ver=14.16
#   nmake /f NMakefile check-msvc-xp
#
# POSIX daemon and host-native POSIX tests remain on the GNU/BSD make paths.

!IFNDEF DAEMON_CPP_DIR
!IF EXIST(src\main.cpp)
DAEMON_CPP_DIR=.
!ELSEIF EXIST(crates\remote-exec-daemon-cpp\src\main.cpp)
DAEMON_CPP_DIR=crates\remote-exec-daemon-cpp
!ELSE
!ERROR Unable to locate remote-exec-daemon-cpp sources. Run nmake from the daemon directory, run from the repository root, or set DAEMON_CPP_DIR.
!ENDIF
!ENDIF

!IFNDEF BUILD_DIR
BUILD_DIR=$(DAEMON_CPP_DIR)\build
!ENDIF

CXX=cl

MSVC_NATIVE_OBJ_DIR=$(BUILD_DIR)\obj\msvc-native
MSVC_NATIVE_TEST_OBJ_DIR=$(BUILD_DIR)\obj\msvc-native-test
MSVC_NATIVE_TARGET=$(BUILD_DIR)\remote-exec-daemon-cpp-msvc.exe
MSVC_NATIVE_BASIC_MUTEX_TEST=$(BUILD_DIR)\test_basic_mutex-msvc.exe
MSVC_NATIVE_PATCH_TEST=$(BUILD_DIR)\test_patch-msvc.exe
MSVC_NATIVE_SESSION_STORE_TEST=$(BUILD_DIR)\test_session_store-msvc.exe
MSVC_NATIVE_TRANSFER_TEST=$(BUILD_DIR)\test_transfer-msvc.exe
MSVC_NATIVE_CONFIG_TEST=$(BUILD_DIR)\test_config-msvc.exe
MSVC_NATIVE_HTTP_REQUEST_TEST=$(BUILD_DIR)\test_http_request-msvc.exe
MSVC_NATIVE_SERVER_TRANSPORT_TEST=$(BUILD_DIR)\test_server_transport-msvc.exe
MSVC_NATIVE_CONNECTION_MANAGER_TEST=$(BUILD_DIR)\test_connection_manager-msvc.exe
MSVC_NATIVE_SERVER_ROUTES_COMMON_TEST=$(BUILD_DIR)\test_server_routes_common-msvc.exe
MSVC_NATIVE_SERVER_RUNTIME_TEST=$(BUILD_DIR)\test_server_runtime-msvc.exe
MSVC_NATIVE_SANDBOX_TEST=$(BUILD_DIR)\test_sandbox-msvc.exe
MSVC_NATIVE_PORT_TUNNEL_FRAME_TEST=$(BUILD_DIR)\test_port_tunnel_frame-msvc.exe

MSVC_XP_OBJ_DIR=$(BUILD_DIR)\obj\msvc-xp
MSVC_XP_TEST_OBJ_DIR=$(BUILD_DIR)\obj\msvc-xp-test
MSVC_XP_TARGET=$(BUILD_DIR)\remote-exec-daemon-cpp-xp-msvc.exe
MSVC_XP_BASIC_MUTEX_TEST=$(BUILD_DIR)\test_basic_mutex-xp-msvc.exe
MSVC_XP_PATCH_TEST=$(BUILD_DIR)\test_patch-xp-msvc.exe
MSVC_XP_SESSION_STORE_TEST=$(BUILD_DIR)\test_session_store-xp-msvc.exe
MSVC_XP_TRANSFER_TEST=$(BUILD_DIR)\test_transfer-xp-msvc.exe
MSVC_XP_CONFIG_TEST=$(BUILD_DIR)\test_config-xp-msvc.exe
MSVC_XP_HTTP_REQUEST_TEST=$(BUILD_DIR)\test_http_request-xp-msvc.exe
MSVC_XP_SERVER_TRANSPORT_TEST=$(BUILD_DIR)\test_server_transport-xp-msvc.exe
MSVC_XP_CONNECTION_MANAGER_TEST=$(BUILD_DIR)\test_connection_manager-xp-msvc.exe
MSVC_XP_SERVER_ROUTES_COMMON_TEST=$(BUILD_DIR)\test_server_routes_common-xp-msvc.exe
MSVC_XP_SERVER_RUNTIME_TEST=$(BUILD_DIR)\test_server_runtime-xp-msvc.exe
MSVC_XP_SANDBOX_TEST=$(BUILD_DIR)\test_sandbox-xp-msvc.exe
MSVC_XP_PORT_TUNNEL_FRAME_TEST=$(BUILD_DIR)\test_port_tunnel_frame-xp-msvc.exe

!IFNDEF MSVC_XP_WINSDK_DIR
MSVC_XP_WINSDK_DIR=C:\Program Files (x86)\Microsoft SDKs\Windows\v7.1A
!ENDIF

MSVC_COMMON_CPPFLAGS=/I"$(DAEMON_CPP_DIR)\include" /I"$(DAEMON_CPP_DIR)\third_party" \
    /DWIN32 /D_WINDOWS \
    /DWIN32_LEAN_AND_MEAN /DUNICODE /D_UNICODE /DNOMINMAX /D_CRT_SECURE_NO_WARNINGS /D_CRT_NONSTDC_NO_DEPRECATE
MSVC_NATIVE_CPPFLAGS=$(MSVC_COMMON_CPPFLAGS)
MSVC_XP_CPPFLAGS=$(MSVC_COMMON_CPPFLAGS) /I"$(MSVC_XP_WINSDK_DIR)\Include" \
    /DWINVER=0x0501 /D_WIN32_WINNT=0x0501 /D_USING_V110_SDK71_

MSVC_CXXFLAGS=/nologo /EHsc /MT /W3 /Od /std:c++14
MSVC_LDLIBS=ws2_32.lib
MSVC_NATIVE_LDFLAGS=/link /NOLOGO /SUBSYSTEM:CONSOLE
MSVC_XP_LDFLAGS=/link /NOLOGO /LIBPATH:"$(MSVC_XP_WINSDK_DIR)\Lib" /SUBSYSTEM:CONSOLE,5.01 /MACHINE:X86

!IFNDEF TEST_LOG_LEVEL
!IFDEF REMOTE_EXEC_LOG
TEST_LOG_LEVEL=$(REMOTE_EXEC_LOG)
!ELSEIFDEF REMOTE_EXEC_TEST_LOG
TEST_LOG_LEVEL=$(REMOTE_EXEC_TEST_LOG)
!ELSE
TEST_LOG_LEVEL=off
!ENDIF
!ENDIF

SOURCE_PREFIX=$(DAEMON_CPP_DIR)/
!include "$(DAEMON_CPP_DIR)\mk\sources.mk"

MSVC_WIN32_DAEMON_SRCS=$(WINDOWS_DAEMON_SRCS)

MSVC_WIN32_BASIC_MUTEX_TEST_SRCS=$(HOST_BASIC_MUTEX_SRCS)

MSVC_WIN32_PATCH_TEST_SRCS=$(HOST_PATCH_SRCS)

MSVC_WIN32_SESSION_STORE_TEST_SRCS=$(WINDOWS_SESSION_STORE_TEST_SRCS)

MSVC_WIN32_TRANSFER_TEST_SRCS=$(HOST_TRANSFER_SRCS)

MSVC_WIN32_CONFIG_TEST_SRCS=$(HOST_CONFIG_SRCS)

MSVC_WIN32_HTTP_REQUEST_TEST_SRCS=$(HOST_HTTP_REQUEST_SRCS)

MSVC_WIN32_SERVER_TRANSPORT_TEST_SRCS=$(WINDOWS_SERVER_TRANSPORT_TEST_SRCS)

MSVC_WIN32_CONNECTION_MANAGER_TEST_SRCS=$(WINDOWS_CONNECTION_MANAGER_TEST_SRCS)

MSVC_WIN32_SERVER_ROUTES_COMMON_TEST_SRCS=$(WINDOWS_SERVER_ROUTES_COMMON_TEST_SRCS)

MSVC_WIN32_SERVER_RUNTIME_TEST_SRCS=$(WINDOWS_SERVER_RUNTIME_TEST_SRCS)

MSVC_WIN32_SANDBOX_TEST_SRCS=$(HOST_SANDBOX_SRCS)

MSVC_WIN32_PORT_TUNNEL_FRAME_TEST_SRCS=$(HOST_PORT_TUNNEL_FRAME_SRCS)

MSVC_NATIVE_TEST_PHONY_TARGETS= \
    test-msvc-native-basic-mutex \
    test-msvc-native-patch \
    test-msvc-native-session-store \
    test-msvc-native-transfer \
    test-msvc-native-config \
    test-msvc-native-http-request \
    test-msvc-native-server-transport \
    test-msvc-native-connection-manager \
    test-msvc-native-server-routes-common \
    test-msvc-native-server-runtime \
    test-msvc-native-sandbox \
    test-msvc-native-port-tunnel-frame

MSVC_XP_TEST_PHONY_TARGETS= \
    test-msvc-xp-basic-mutex \
    test-msvc-xp-patch \
    test-msvc-xp-session-store \
    test-msvc-xp-transfer \
    test-msvc-xp-config \
    test-msvc-xp-http-request \
    test-msvc-xp-server-transport \
    test-msvc-xp-connection-manager \
    test-msvc-xp-server-routes-common \
    test-msvc-xp-server-runtime \
    test-msvc-xp-sandbox \
    test-msvc-xp-port-tunnel-frame

all: all-msvc-native

all-msvc-native: $(MSVC_NATIVE_TARGET)

all-msvc-xp: $(MSVC_XP_TARGET)

test-msvc-native: $(MSVC_NATIVE_TEST_PHONY_TARGETS)

test-msvc-native-basic-mutex: $(MSVC_NATIVE_BASIC_MUTEX_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_BASIC_MUTEX_TEST)"

test-msvc-native-patch: $(MSVC_NATIVE_PATCH_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_PATCH_TEST)"

test-msvc-native-session-store: $(MSVC_NATIVE_SESSION_STORE_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_SESSION_STORE_TEST)"

test-msvc-native-transfer: $(MSVC_NATIVE_TRANSFER_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_TRANSFER_TEST)"

test-msvc-native-config: $(MSVC_NATIVE_CONFIG_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_CONFIG_TEST)"

test-msvc-native-http-request: $(MSVC_NATIVE_HTTP_REQUEST_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_HTTP_REQUEST_TEST)"

test-msvc-native-server-transport: $(MSVC_NATIVE_SERVER_TRANSPORT_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_SERVER_TRANSPORT_TEST)"

test-msvc-native-connection-manager: $(MSVC_NATIVE_CONNECTION_MANAGER_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_CONNECTION_MANAGER_TEST)"

test-msvc-native-server-routes-common: $(MSVC_NATIVE_SERVER_ROUTES_COMMON_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_SERVER_ROUTES_COMMON_TEST)"

test-msvc-native-server-runtime: $(MSVC_NATIVE_SERVER_RUNTIME_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_SERVER_RUNTIME_TEST)"

test-msvc-native-sandbox: $(MSVC_NATIVE_SANDBOX_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_SANDBOX_TEST)"

test-msvc-native-port-tunnel-frame: $(MSVC_NATIVE_PORT_TUNNEL_FRAME_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_NATIVE_PORT_TUNNEL_FRAME_TEST)"

test-msvc-xp: $(MSVC_XP_TEST_PHONY_TARGETS)

test-msvc-xp-basic-mutex: $(MSVC_XP_BASIC_MUTEX_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_BASIC_MUTEX_TEST)"

test-msvc-xp-patch: $(MSVC_XP_PATCH_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_PATCH_TEST)"

test-msvc-xp-session-store: $(MSVC_XP_SESSION_STORE_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_SESSION_STORE_TEST)"

test-msvc-xp-transfer: $(MSVC_XP_TRANSFER_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_TRANSFER_TEST)"

test-msvc-xp-config: $(MSVC_XP_CONFIG_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_CONFIG_TEST)"

test-msvc-xp-http-request: $(MSVC_XP_HTTP_REQUEST_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_HTTP_REQUEST_TEST)"

test-msvc-xp-server-transport: $(MSVC_XP_SERVER_TRANSPORT_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_SERVER_TRANSPORT_TEST)"

test-msvc-xp-connection-manager: $(MSVC_XP_CONNECTION_MANAGER_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_CONNECTION_MANAGER_TEST)"

test-msvc-xp-server-routes-common: $(MSVC_XP_SERVER_ROUTES_COMMON_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_SERVER_ROUTES_COMMON_TEST)"

test-msvc-xp-server-runtime: $(MSVC_XP_SERVER_RUNTIME_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_SERVER_RUNTIME_TEST)"

test-msvc-xp-sandbox: $(MSVC_XP_SANDBOX_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_SANDBOX_TEST)"

test-msvc-xp-port-tunnel-frame: $(MSVC_XP_PORT_TUNNEL_FRAME_TEST)
	set "REMOTE_EXEC_LOG=$(TEST_LOG_LEVEL)" && "$(MSVC_XP_PORT_TUNNEL_FRAME_TEST)"

check-msvc-native: all-msvc-native test-msvc-native

check-msvc-xp: all-msvc-xp test-msvc-xp

$(MSVC_NATIVE_TARGET): $(MSVC_WIN32_DAEMON_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_OBJ_DIR)" mkdir "$(MSVC_NATIVE_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_DAEMON_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_BASIC_MUTEX_TEST): $(MSVC_WIN32_BASIC_MUTEX_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_BASIC_MUTEX_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_PATCH_TEST): $(MSVC_WIN32_PATCH_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_PATCH_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_SESSION_STORE_TEST): $(MSVC_WIN32_SESSION_STORE_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SESSION_STORE_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_TRANSFER_TEST): $(MSVC_WIN32_TRANSFER_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_TRANSFER_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_CONFIG_TEST): $(MSVC_WIN32_CONFIG_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_CONFIG_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_HTTP_REQUEST_TEST): $(MSVC_WIN32_HTTP_REQUEST_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_HTTP_REQUEST_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_SERVER_TRANSPORT_TEST): $(MSVC_WIN32_SERVER_TRANSPORT_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SERVER_TRANSPORT_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_CONNECTION_MANAGER_TEST): $(MSVC_WIN32_CONNECTION_MANAGER_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_CONNECTION_MANAGER_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_SERVER_ROUTES_COMMON_TEST): $(MSVC_WIN32_SERVER_ROUTES_COMMON_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SERVER_ROUTES_COMMON_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_SERVER_RUNTIME_TEST): $(MSVC_WIN32_SERVER_RUNTIME_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SERVER_RUNTIME_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_SANDBOX_TEST): $(MSVC_WIN32_SANDBOX_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SANDBOX_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_NATIVE_PORT_TUNNEL_FRAME_TEST): $(MSVC_WIN32_PORT_TUNNEL_FRAME_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" mkdir "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_NATIVE_CPPFLAGS) /Fo"$(MSVC_NATIVE_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_PORT_TUNNEL_FRAME_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_NATIVE_LDFLAGS)

$(MSVC_XP_TARGET): $(MSVC_WIN32_DAEMON_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_OBJ_DIR)" mkdir "$(MSVC_XP_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_DAEMON_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_BASIC_MUTEX_TEST): $(MSVC_WIN32_BASIC_MUTEX_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_BASIC_MUTEX_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_PATCH_TEST): $(MSVC_WIN32_PATCH_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_PATCH_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_SESSION_STORE_TEST): $(MSVC_WIN32_SESSION_STORE_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SESSION_STORE_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_TRANSFER_TEST): $(MSVC_WIN32_TRANSFER_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_TRANSFER_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_CONFIG_TEST): $(MSVC_WIN32_CONFIG_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_CONFIG_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_HTTP_REQUEST_TEST): $(MSVC_WIN32_HTTP_REQUEST_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_HTTP_REQUEST_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_SERVER_TRANSPORT_TEST): $(MSVC_WIN32_SERVER_TRANSPORT_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SERVER_TRANSPORT_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_CONNECTION_MANAGER_TEST): $(MSVC_WIN32_CONNECTION_MANAGER_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_CONNECTION_MANAGER_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_SERVER_ROUTES_COMMON_TEST): $(MSVC_WIN32_SERVER_ROUTES_COMMON_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SERVER_ROUTES_COMMON_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_SERVER_RUNTIME_TEST): $(MSVC_WIN32_SERVER_RUNTIME_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SERVER_RUNTIME_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_SANDBOX_TEST): $(MSVC_WIN32_SANDBOX_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_SANDBOX_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

$(MSVC_XP_PORT_TUNNEL_FRAME_TEST): $(MSVC_WIN32_PORT_TUNNEL_FRAME_TEST_SRCS)
	@if not exist "$(BUILD_DIR)" mkdir "$(BUILD_DIR)"
	@if not exist "$(BUILD_DIR)\obj" mkdir "$(BUILD_DIR)\obj"
	@if not exist "$(MSVC_XP_TEST_OBJ_DIR)" mkdir "$(MSVC_XP_TEST_OBJ_DIR)"
	$(CXX) $(MSVC_CXXFLAGS) $(MSVC_XP_CPPFLAGS) /Fo"$(MSVC_XP_TEST_OBJ_DIR)\\" /Fe"$@" $(MSVC_WIN32_PORT_TUNNEL_FRAME_TEST_SRCS) $(MSVC_LDLIBS) $(MSVC_XP_LDFLAGS)

clean:
	@if exist "$(MSVC_NATIVE_TARGET)" del /Q "$(MSVC_NATIVE_TARGET)"
	@if exist "$(MSVC_NATIVE_BASIC_MUTEX_TEST)" del /Q "$(MSVC_NATIVE_BASIC_MUTEX_TEST)"
	@if exist "$(MSVC_NATIVE_PATCH_TEST)" del /Q "$(MSVC_NATIVE_PATCH_TEST)"
	@if exist "$(MSVC_NATIVE_SESSION_STORE_TEST)" del /Q "$(MSVC_NATIVE_SESSION_STORE_TEST)"
	@if exist "$(MSVC_NATIVE_TRANSFER_TEST)" del /Q "$(MSVC_NATIVE_TRANSFER_TEST)"
	@if exist "$(MSVC_NATIVE_CONFIG_TEST)" del /Q "$(MSVC_NATIVE_CONFIG_TEST)"
	@if exist "$(MSVC_NATIVE_HTTP_REQUEST_TEST)" del /Q "$(MSVC_NATIVE_HTTP_REQUEST_TEST)"
	@if exist "$(MSVC_NATIVE_SERVER_TRANSPORT_TEST)" del /Q "$(MSVC_NATIVE_SERVER_TRANSPORT_TEST)"
	@if exist "$(MSVC_NATIVE_CONNECTION_MANAGER_TEST)" del /Q "$(MSVC_NATIVE_CONNECTION_MANAGER_TEST)"
	@if exist "$(MSVC_NATIVE_SERVER_ROUTES_COMMON_TEST)" del /Q "$(MSVC_NATIVE_SERVER_ROUTES_COMMON_TEST)"
	@if exist "$(MSVC_NATIVE_SERVER_RUNTIME_TEST)" del /Q "$(MSVC_NATIVE_SERVER_RUNTIME_TEST)"
	@if exist "$(MSVC_NATIVE_SANDBOX_TEST)" del /Q "$(MSVC_NATIVE_SANDBOX_TEST)"
	@if exist "$(MSVC_NATIVE_PORT_TUNNEL_FRAME_TEST)" del /Q "$(MSVC_NATIVE_PORT_TUNNEL_FRAME_TEST)"
	@if exist "$(MSVC_XP_TARGET)" del /Q "$(MSVC_XP_TARGET)"
	@if exist "$(MSVC_XP_BASIC_MUTEX_TEST)" del /Q "$(MSVC_XP_BASIC_MUTEX_TEST)"
	@if exist "$(MSVC_XP_PATCH_TEST)" del /Q "$(MSVC_XP_PATCH_TEST)"
	@if exist "$(MSVC_XP_SESSION_STORE_TEST)" del /Q "$(MSVC_XP_SESSION_STORE_TEST)"
	@if exist "$(MSVC_XP_TRANSFER_TEST)" del /Q "$(MSVC_XP_TRANSFER_TEST)"
	@if exist "$(MSVC_XP_CONFIG_TEST)" del /Q "$(MSVC_XP_CONFIG_TEST)"
	@if exist "$(MSVC_XP_HTTP_REQUEST_TEST)" del /Q "$(MSVC_XP_HTTP_REQUEST_TEST)"
	@if exist "$(MSVC_XP_SERVER_TRANSPORT_TEST)" del /Q "$(MSVC_XP_SERVER_TRANSPORT_TEST)"
	@if exist "$(MSVC_XP_CONNECTION_MANAGER_TEST)" del /Q "$(MSVC_XP_CONNECTION_MANAGER_TEST)"
	@if exist "$(MSVC_XP_SERVER_ROUTES_COMMON_TEST)" del /Q "$(MSVC_XP_SERVER_ROUTES_COMMON_TEST)"
	@if exist "$(MSVC_XP_SERVER_RUNTIME_TEST)" del /Q "$(MSVC_XP_SERVER_RUNTIME_TEST)"
	@if exist "$(MSVC_XP_SANDBOX_TEST)" del /Q "$(MSVC_XP_SANDBOX_TEST)"
	@if exist "$(MSVC_XP_PORT_TUNNEL_FRAME_TEST)" del /Q "$(MSVC_XP_PORT_TUNNEL_FRAME_TEST)"
	@if exist "$(MSVC_NATIVE_OBJ_DIR)" rmdir /S /Q "$(MSVC_NATIVE_OBJ_DIR)"
	@if exist "$(MSVC_NATIVE_TEST_OBJ_DIR)" rmdir /S /Q "$(MSVC_NATIVE_TEST_OBJ_DIR)"
	@if exist "$(MSVC_XP_OBJ_DIR)" rmdir /S /Q "$(MSVC_XP_OBJ_DIR)"
	@if exist "$(MSVC_XP_TEST_OBJ_DIR)" rmdir /S /Q "$(MSVC_XP_TEST_OBJ_DIR)"

.PHONY: all all-msvc-native all-msvc-xp test-msvc-native test-msvc-native-basic-mutex test-msvc-native-patch test-msvc-native-session-store test-msvc-native-transfer test-msvc-native-config test-msvc-native-http-request test-msvc-native-server-transport test-msvc-native-connection-manager test-msvc-native-server-routes-common test-msvc-native-server-runtime test-msvc-native-sandbox test-msvc-native-port-tunnel-frame test-msvc-xp test-msvc-xp-basic-mutex test-msvc-xp-patch test-msvc-xp-session-store test-msvc-xp-transfer test-msvc-xp-config test-msvc-xp-http-request test-msvc-xp-server-transport test-msvc-xp-connection-manager test-msvc-xp-server-routes-common test-msvc-xp-server-runtime test-msvc-xp-sandbox test-msvc-xp-port-tunnel-frame check-msvc-native check-msvc-xp clean
